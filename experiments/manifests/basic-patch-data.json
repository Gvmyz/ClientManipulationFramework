{
  "name": "basic-patch-data",
  "description": "External memory patching: change TestTarget's stack-local `value` variable from 600 to 9999 using WriteProcessMemory from a separate process. No code injection, no new thread or DLL inside the target. The expected outcome with the current ETW provider list is an empty trace — the kernel-process provider does not surface cross-process memory writes. That absence is itself the finding.",
  "providerGuid": "{22fb2cd6-0e7b-422b-a0c7-2fad1fd0e716}",
  "sessionName": "ClientManipulationFramework",
  "timings": {
    "warmupSeconds": 3,
    "cooldownSeconds": 3
  },
  "target": {
    "executable": "ProcessToolkit\\x64\\Debug\\TestTarget.exe",
    "arguments": "",
    "workingDirectory": "ProcessToolkit\\x64\\Debug"
  },
  "telemetry": {
    "executable": "ProcessToolkit\\x64\\Debug\\Telemetry.exe",
    "workingDirectory": "ProcessToolkit\\x64\\Debug"
  },
  "manipulation": {
    "executable": "ProcessToolkit\\x64\\Debug\\ProcessToolkit.exe",
    "workingDirectory": "ProcessToolkit\\x64\\Debug",
    "commandLineTemplate": "patch-memory --pid {targetPid} --address {targetInfo.value_addr} --bytes 0F270000 --verify"
  },
  "metadata": {
    "label": "attack",
    "technique": "memorypatch",
    "target": "TestTarget.exe",
    "extra": {
      "variant": "data",
      "notes": "Writes 0F270000 (= 9999 as little-endian int32) into TestTarget's `value` variable. Press SPACE after the run to observe value=10000, confirming the patch landed. --verify reads the bytes back to confirm the write."
    }
  }
}
